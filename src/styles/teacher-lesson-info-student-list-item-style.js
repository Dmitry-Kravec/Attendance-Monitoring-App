import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginTop: 3,
        width: 300,
        height: 40,
        display: "flex",
        justifyContent: "space-around",
        alignItems: "center",
        backgroundColor: "#CFFEAA",
        borderWidth: 1,
        borderColor: "#D5F493",
        marginBottom: 1,
    },
});

export default styles;
