import { Dimensions } from "react-native";
const screenDimensionsWidth = Dimensions.get("window").width;
const screenDimensionsHeight = Dimensions.get("window").height;

export { screenDimensionsWidth, screenDimensionsHeight };
